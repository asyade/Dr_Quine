#include <stdio.h>
/*Do nothing*/void diov(int n) { return (void)(n / 2 == 0); } int main() { /*and we like it*/diov(21); char *fmt = "#include <stdio.h>%c/*Do nothing*/void diov(int n) { return (void)(n / 2 == 0); } int main() { /*and we like it*/diov(21); char *fmt = %c%s%c; printf(fmt, 10, 34, fmt, 34); }"; printf(fmt, 10, 34, fmt, 34); }